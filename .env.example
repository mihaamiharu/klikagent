PORT=3000

# GitHub
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret_here
GITHUB_TOKEN=your_github_pat_here          # needs: contents r/w, pull-requests r/w, issues read, actions r/w
GITHUB_OWNER=your_github_username_or_org
GITHUB_TEST_REPO=klikagent-tests

# Self-correction loop
KLIKAGENT_TESTS_LOCAL_PATH=/opt/klikagent-tests
MAX_SELF_CORRECTION_ATTEMPTS=2
GITHUB_MAIN_REPO=your-dev-repo             # repo where GitHub Issues live (triggers flow1/flow2)
GITHUB_TEST_REPO=klikagent-tests           # QA test repo where specs are committed

# AI (Minimax or any OpenAI-compatible provider)
AI_API_KEY=your_ai_api_key_here
AI_BASE_URL=https://api.minimax.io/v1
AI_MODEL=MiniMax-M2.7

# Crawler — QA environment the enrichment agent crawls
QA_BASE_URL=https://your-qa-environment.com

# Personas — keys must match config/personas.json in klikagent-tests repo
# All use Password123!
QA_PATIENT_EMAIL=jane.doe@caresync.dev
QA_PATIENT_PASSWORD=Password123!
QA_DOCTOR_EMAIL=dr.smith@caresync.dev
QA_DOCTOR_PASSWORD=Password123!
QA_ADMIN_EMAIL=admin@caresync.dev
QA_ADMIN_PASSWORD=Password123!
